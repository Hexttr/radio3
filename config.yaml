# AI Radio — конфигурация

# Язык (ru | en | tj): диджей, новости, погода
language: "ru"

# Папка с музыкой (mp3)
music_dir: "music"

# Папка кэша озвучек (TTS)
cache_dir: "cache"

# Region (city for weather and news)
region:
  city: "Dushanbe"
  timezone: "Asia/Dushanbe"
  latitude: 38.56
  longitude: 68.78

# Интервалы (минуты)
intervals:
  news_minutes: 180
  weather_minutes: 240
  # Новости: кол-во новостей в выпуске (заголовок + содержание ~800 символов)
  news_items: 8

# TTS — provider: "edge" (free) | "elevenlabs"
tts:
  provider: "elevenlabs"
  # Усиление голоса (dB), чтобы TTS не уступал музыке. +10 — заметно громче
  volume_boost_db: 10
  # Edge settings
  voice: "en-GB-SoniaNeural"
  rate: "+0%"
  volume: "+0%"
  # ElevenLabs (when provider: elevenlabs)
  voice_id: "21m00Tcm4TlvDq8ikWAM"
  model_id: "eleven_multilingual_v2"
  api_key_env: "ELEVENLABS_API_KEY"

# AI Диджей — Groq (бесплатный тариф)
# Получить ключ: https://console.groq.com
ai:
  provider: "groq"
  model: "llama-3.1-8b-instant"
  api_key_env: "GROQ_API_KEY"

# Порты
server:
  host: "0.0.0.0"
  port: 5000
